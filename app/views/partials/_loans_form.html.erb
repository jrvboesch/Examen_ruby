

      <%= f.label :name, "Nombre" %>
      <%= f.text_field :name %>

      <%= f.label :loan, "Prestamo" %>
      <%= f.text_field :loan %>

      <%= f.label :loantype, "Tipo" %>
      <%= f.select(:loantype, @loantypes, :require => true ) %>


      <%= f.submit "Crear Prestamo", class: "btn btn-primary" %>